name: Prepare K-Mate v42 loader

on:
  push:
    branches: [kmate-personalization-v42]
    paths:
      - ".github/workflows/kmate-v42-prepare.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-loader:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: kmate-personalization-v42
          fetch-depth: 0
      - name: Point the public page at v42
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('kmate-trainer/index.html')
          text = path.read_text()
          old = 'app-v7.js?v=41.0.0'
          new = 'app-v7.js?v=42.0.0'
          if old not in text and new not in text:
              raise SystemExit('Expected K-Mate loader was not found.')
          path.write_text(text.replace(old, new))
          PY
      - name: Commit the loader update
        run: |
          git config user.name "K-Mate release bot"
          git config user.email "actions@users.noreply.github.com"
          git add kmate-trainer/index.html
          if git diff --cached --quiet; then
            echo "Loader already points at v42."
            exit 0
          fi
          git commit -m "Point K-Mate page at v42"
          git push origin HEAD:kmate-personalization-v42
