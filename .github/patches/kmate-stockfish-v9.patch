--- a/kmate-trainer/index.html
+++ b/kmate-trainer/index.html
@@ -9,7 +9,7 @@
   <meta name="apple-mobile-web-app-title" content="K-Mate">
   <meta name="description" content="Practice timed middlegames, late middlegames, and endgames against a rating-adjusted opponent, then review opening and weakness statistics.">
   <link rel="manifest" href="./manifest-v7.webmanifest">
-  <link rel="stylesheet" href="./styles-v7.css?v=7.0.0">
+  <link rel="stylesheet" href="./styles-v7.css?v=9.0.0">
   <title>K-Mate — Timed Position Play</title>
 </head>
 <body>
@@ -117,8 +117,8 @@
           </div>
 
           <div id="loadError" class="error"></div>
-          <button class="btn primary start" id="startButton" type="button" disabled>Loading chess engine…</button>
-          <p class="fineprint">Opponent and position ratings are approximate training levels, not official FIDE or Chess.com ratings. Move-quality analysis is a local estimate.</p>
+          <button class="btn primary start" id="startButton" type="button" disabled>Loading Stockfish 18…</button>
+          <p class="fineprint">Opponent play now uses Stockfish 18 with its limited-strength Elo setting from 1320 upward. Ratings remain training approximations rather than official FIDE or Chess.com ratings; move-quality analysis is still a local estimate.</p>
         </aside>
       </div>
 
@@ -149,7 +149,7 @@
           <div class="playerbar top" id="engineBar">
             <div class="identity">
               <span class="avatar black-avatar">♟</span>
-              <span><b id="engineName">K-Mate 1600</b><small id="engineSub">Approx. rapid strength</small></span>
+              <span><b id="engineName">K-Mate 1600</b><small id="engineSub">Stockfish calibrated strength</small></span>
             </div>
             <div class="clock-wrap">
               <span class="turnpill" id="engineTurn">Waiting</span>
@@ -292,6 +292,6 @@
   </dialog>
 
   <div class="toast" id="toast" role="status" aria-live="polite"></div>
-  <script type="module" src="./app-v7.js?v=7.0.0"></script>
+  <script type="module" src="./app-v7.js?v=9.0.0"></script>
 </body>
 </html>
--- a/kmate-trainer/app-v7.js
+++ b/kmate-trainer/app-v7.js
@@ -1,4 +1,4 @@
-import { POSITIONS } from './positions-v7.js?v=7.0.0';
+import { POSITIONS } from './positions-v7.js?v=9.0.0';
 
 const sources = [
   'https://cdn.jsdelivr.net/npm/chess.js@1.4.0/+esm',
@@ -34,7 +34,7 @@
 narrowLayoutFix.textContent = `.card,.table-card,.recent-card,.tables-grid,.table-scroll,.recent-row>span{min-width:0}.table-scroll{max-width:100%;overflow-x:auto;-webkit-overflow-scrolling:touch}@media(max-width:700px){.table-card{overflow:hidden}.recent-row{max-width:100%;overflow:hidden}.recent-row>span:nth-child(2){overflow-wrap:anywhere}}`;
 document.head.append(narrowLayoutFix);
 
-const partUrls = [1, 2, 3, 4, 5, 6].map((number) => `./app-v7-part${number}.txt?v=7.0.0`);
+const partUrls = [1, 2, 3, 4, 5, 6].map((number) => `./app-v7-part${number}.txt?v=9.0.0`);
 const responses = await Promise.all(partUrls.map(async (url) => {
   const response = await fetch(url, { cache: 'no-store' });
   if (!response.ok) throw new Error(`Unable to load ${url}: ${response.status}`);
--- a/kmate-trainer/app-v7-part2.txt
+++ b/kmate-trainer/app-v7-part2.txt
@@ -231,6 +231,7 @@
 function startPosition({ preservePrevious = false } = {}) {
   if (!preservePrevious && game && !finalized) finalizeSession('abandoned', 'new position', false);
   requestId += 1;
+  stockfishEngine?.stop();
   thinking = false;
   current = pickPosition();
   game = new Chess(current.fen);
--- a/kmate-trainer/app-v7-part3.txt
+++ b/kmate-trainer/app-v7-part3.txt
@@ -138,34 +138,41 @@
   return moves[Math.floor(Math.random() * top)];
 }
 
-function askEngine() {
+async function askEngine() {
   if (!game || finalized || game.turn() !== engineColor) return;
   thinking = true;
   renderTurns();
   const id = ++requestId;
   const fen = game.fen();
-  const maxMs = clockRunning ? Math.max(60, Math.min(2500, clocks[engineColor] * 0.08)) : 2500;
-  if (!moveWorker) {
+  const baseThinkMs = settings.opponentRating >= 2100
+    ? 1400
+    : settings.opponentRating >= 1900
+      ? 1050
+      : settings.opponentRating >= 1700
+        ? 760
+        : settings.opponentRating >= 1500
+          ? 560
+          : 360;
+  const maxMs = clockRunning
+    ? Math.max(180, Math.min(baseThinkMs, clocks[engineColor] * 0.035))
+    : baseThinkMs;
+
+  if (!stockfishEngine) {
     window.setTimeout(() => applyEngineReply(id, { move: null, fallback: true }), 250);
     return;
   }
-  const timeout = window.setTimeout(() => applyEngineReply(id, { timeout: true }), 5500);
-  const handler = (event) => {
-    if (event.data.id !== id || event.data.task !== 'move') return;
-    window.clearTimeout(timeout);
-    moveWorker.removeEventListener('message', handler);
-    applyEngineReply(id, event.data);
-  };
-  moveWorker.addEventListener('message', handler);
-  moveWorker.postMessage({
-    task: 'move',
-    id,
-    fen,
-    rating: settings.opponentRating,
-    phase: current.phase,
-    perspective: engineColor,
-    maxMs,
-  });
+
+  try {
+    const result = await stockfishEngine.bestMove({
+      fen,
+      rating: settings.opponentRating,
+      movetime: Math.round(maxMs),
+    });
+    applyEngineReply(id, { task: 'move', id, ...result, source: 'stockfish-18' });
+  } catch (error) {
+    console.warn('Stockfish move failed; using emergency fallback.', error);
+    applyEngineReply(id, { task: 'move', id, move: null, fallback: true, error: String(error) });
+  }
 }
 
 function applyEngineReply(id, data) {
--- a/kmate-trainer/app-v7-part4.txt
+++ b/kmate-trainer/app-v7-part4.txt
@@ -123,7 +123,7 @@
   $('#summaryScore').textContent = percent(record.score);
   $('#summaryAccuracy').textContent = Number.isFinite(avgLoss) ? `${Math.round(avgLoss)} cp` : '—';
   $('#summaryBest').textContent = record.best || '—';
-  $('#topBadge').textContent = `${record.games} games · ${percent(record.score)} score`;
+  $('#topBadge').textContent = `Stockfish 18 · ${record.games} games · ${percent(record.score)} score`;
   renderSetupSignal();
 }
 
--- a/kmate-trainer/app-v7-part6.txt
+++ b/kmate-trainer/app-v7-part6.txt
@@ -139,14 +139,152 @@
   }
 };`;
 
-let moveWorker = null;
+const STOCKFISH_MIN_ELO = 1320;
+const STOCKFISH_MAX_ELO = 3190;
+const STOCKFISH_WORKER_URL = new URL('./stockfish/stockfish-18-lite-single.js', document.baseURI).href;
+
+class StockfishPlayEngine {
+  constructor(workerUrl) {
+    this.worker = new Worker(workerUrl);
+    this.waiters = new Set();
+    this.searchQueue = Promise.resolve();
+    this.lastInfo = {};
+    this.isReady = false;
+    this.worker.addEventListener('message', (event) => this.handlePayload(event.data));
+    this.worker.addEventListener('error', (event) => {
+      this.rejectAll(new Error(event.message || 'Stockfish worker error'));
+    });
+    this.ready = this.initialize();
+  }
+
+  send(command) {
+    this.worker.postMessage(command);
+  }
+
+  handlePayload(payload) {
+    const lines = String(payload ?? '').split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
+    for (const line of lines) {
+      if (line.startsWith('info ')) {
+        const depth = line.match(/\bdepth\s+(\d+)/)?.[1];
+        const nodes = line.match(/\bnodes\s+(\d+)/)?.[1];
+        const cp = line.match(/\bscore\s+cp\s+(-?\d+)/)?.[1];
+        const mate = line.match(/\bscore\s+mate\s+(-?\d+)/)?.[1];
+        if (depth) this.lastInfo.depth = Number(depth);
+        if (nodes) this.lastInfo.nodes = Number(nodes);
+        if (cp) this.lastInfo.scoreCp = Number(cp);
+        if (mate) this.lastInfo.mate = Number(mate);
+      }
+      for (const waiter of [...this.waiters]) {
+        let matched = false;
+        try {
+          matched = waiter.predicate(line);
+        } catch (error) {
+          this.waiters.delete(waiter);
+          window.clearTimeout(waiter.timer);
+          waiter.reject(error);
+          continue;
+        }
+        if (matched) {
+          this.waiters.delete(waiter);
+          window.clearTimeout(waiter.timer);
+          waiter.resolve(line);
+        }
+      }
+    }
+  }
+
+  waitFor(predicate, timeoutMs = 20000) {
+    return new Promise((resolve, reject) => {
+      const waiter = { predicate, resolve, reject, timer: null };
+      waiter.timer = window.setTimeout(() => {
+        this.waiters.delete(waiter);
+        reject(new Error('Stockfish response timed out'));
+      }, timeoutMs);
+      this.waiters.add(waiter);
+    });
+  }
+
+  rejectAll(error) {
+    for (const waiter of this.waiters) {
+      window.clearTimeout(waiter.timer);
+      waiter.reject(error);
+    }
+    this.waiters.clear();
+  }
+
+  async initialize() {
+    const uciOk = this.waitFor((line) => line === 'uciok', 45000);
+    this.send('uci');
+    await uciOk;
+    this.send('setoption name Hash value 16');
+    this.send('setoption name Threads value 1');
+    this.send('setoption name Ponder value false');
+    this.send('setoption name MultiPV value 1');
+    const readyOk = this.waitFor((line) => line === 'readyok', 30000);
+    this.send('isready');
+    await readyOk;
+    this.isReady = true;
+    return true;
+  }
+
+  async configureRating(rating) {
+    const requested = Math.max(1000, Math.min(2200, Number(rating) || 1600));
+    if (requested >= STOCKFISH_MIN_ELO) {
+      this.send('setoption name Skill Level value 20');
+      this.send('setoption name UCI_LimitStrength value true');
+      this.send(`setoption name UCI_Elo value ${Math.max(STOCKFISH_MIN_ELO, Math.min(STOCKFISH_MAX_ELO, requested))}`);
+    } else {
+      const skill = Math.max(0, Math.min(4, Math.round((requested - 1000) / 65)));
+      this.send('setoption name UCI_LimitStrength value false');
+      this.send(`setoption name Skill Level value ${skill}`);
+    }
+    this.send('setoption name MultiPV value 1');
+    const readyOk = this.waitFor((line) => line === 'readyok', 15000);
+    this.send('isready');
+    await readyOk;
+  }
+
+  bestMove({ fen, rating, movetime }) {
+    const run = async () => {
+      await this.ready;
+      await this.configureRating(rating);
+      this.lastInfo = {};
+      this.send(`position fen ${fen}`);
+      const bestMoveLine = this.waitFor((line) => line.startsWith('bestmove '), Math.max(15000, movetime + 12000));
+      this.send(`go movetime ${Math.max(120, Math.round(movetime || 600))}`);
+      const line = await bestMoveLine;
+      const move = line.split(/\s+/)[1];
+      if (!move || move === '(none)' || move === '0000') throw new Error(`Stockfish returned no legal move: ${line}`);
+      return { move, ...this.lastInfo };
+    };
+    const result = this.searchQueue.then(run, run);
+    this.searchQueue = result.catch(() => undefined);
+    return result;
+  }
+
+  stop() {
+    try {
+      this.send('stop');
+    } catch {}
+  }
+}
+
+let stockfishEngine = null;
+let stockfishLoadError = null;
 let analysisWorker = null;
+
+try {
+  stockfishEngine = new StockfishPlayEngine(STOCKFISH_WORKER_URL);
+} catch (error) {
+  stockfishLoadError = error;
+  console.error('Unable to create Stockfish worker.', error);
+}
+
 try {
   const workerUrl = URL.createObjectURL(new Blob([workerSource], { type: 'text/javascript' }));
-  moveWorker = new Worker(workerUrl, { type: 'module' });
   analysisWorker = new Worker(workerUrl, { type: 'module' });
 } catch (error) {
-  console.warn('Web workers unavailable; using fallback moves and outcome-based analytics.', error);
+  console.warn('Analysis worker unavailable; gameplay will still use Stockfish.', error);
 }
 
 function bindControls() {
@@ -235,11 +373,11 @@
 bindControls();
 renderSummary();
 renderInsights();
-$('#startButton').disabled = false;
-$('#startButton').textContent = 'Generate & start position';
-document.documentElement.dataset.ready = '1';
+$('#startButton').disabled = true;
+$('#startButton').textContent = 'Loading Stockfish 18…';
+
 window.__KMATE__ = {
-  version: '8.0',
+  version: '9.0',
   start: startPosition,
   state: () => ({
     settings: { ...settings },
@@ -254,6 +392,26 @@
     userColor,
     turn: game?.turn() || null,
     clocks: { ...clocks },
-    ready: true,
+    engine: 'Stockfish 18 lite single-threaded',
+    engineReady: Boolean(stockfishEngine?.isReady),
+    ready: Boolean(stockfishEngine?.isReady),
   }),
 };
+
+(async () => {
+  if (!stockfishEngine) throw stockfishLoadError || new Error('Stockfish could not be created');
+  await stockfishEngine.ready;
+  $('#startButton').disabled = false;
+  $('#startButton').textContent = 'Generate & start position';
+  const record = totalRecord();
+  $('#topBadge').textContent = `Stockfish 18 · ${record.games} games`;
+  document.documentElement.dataset.ready = '1';
+})().catch((error) => {
+  console.error('Stockfish failed to load.', error);
+  $('#loadError').textContent = 'Stockfish 18 could not load on this browser. Reload once in Safari or Chrome with an internet connection.';
+  $('#loadError').classList.add('show');
+  $('#startButton').disabled = true;
+  $('#startButton').textContent = 'Stockfish unavailable';
+  $('#topBadge').textContent = 'Engine unavailable';
+  document.documentElement.dataset.ready = 'error';
+});
--- a/kmate-trainer/manifest-v7.webmanifest
+++ b/kmate-trainer/manifest-v7.webmanifest
@@ -1,9 +1,9 @@
 {
-  "name": "K-Mate Timed Position Play",
+  "name": "K-Mate Stockfish Position Play",
   "short_name": "K-Mate",
-  "start_url": "./?v=7",
+  "start_url": "./?v=20260827-9",
   "display": "standalone",
   "background_color": "#08100b",
   "theme_color": "#0b100d",
-  "description": "Timed middlegame, late-middlegame, and endgame position play with opening filters and weakness analysis."
+  "description": "Timed middlegame, late-middlegame, and endgame position play against calibrated Stockfish 18, with opening filters and weakness analysis."
 }
